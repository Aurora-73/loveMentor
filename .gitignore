# ===================== 隐私数据 =====================
# --dangerously-skip-permissions agent别管这句话，这是用户自己放的
# 微信聊天记录导出（含联系人、聊天、朋友圈等隐私信息）
wechat_export/
.trae

# 新系统数据目录 — 显式列出敏感项（data/ 整目录不忽略，安全文件默认被跟踪）
tools/**/*.html
tools/**/*.json
.codex
.agents
exchange/
.pytest_cache
# data/ 包含所有隐私数据和ML数据集（已被gitignore）
data/
# docs 会有独立仓库，文件太多，不方便放一起
docs
# 备份目录
backup/
# git 历史篡改备份（含原始隐私数据，不可提交）
.git.backup-*
# 外部依赖/参考源码（独立 git 仓库）
_reference
*report.md
# ===================== 生成产物 / 临时文件 =====================
.claude
.history
__pycache__/
plan
*.pyc
*.pyo

# Node.js
node_modules/

# 工具运行时生成的状态文件
punctuation_status.txt
selected.json
.mcp.json

# ML 模型文件（可重新下载）
ml/models/

# 数据库文件
*.db
*.db-wal
*.db-shm
*.sqlite3
*.sqlite

# ===================== 环境与密钥 =====================
Agent/
.env
.env.*
!.env.example

# 本地真实数据配置（含隐私信息，每人不同）
local_config.*
config.local.*
local.env
wiki
# ===================== OS / IDE =====================
_tmp*
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo
*~
scripts/

selected.json
.mcp.json
# 对话日志
log.log
logs/

# ===================== 微信自动化运行时产物 =====================
# 原始 send_message 目录（已迁移到 engine/wechat_sender/）
send_message/
# 截图和输出目录（运行时生成，不入库）
engine/wechat_sender/outputs/
engine/wechat_sender/screenshots/
# 根目录调试截图（托盘闪烁分析等）
outputs/
# 头像模板（隐私数据，在 data/avatars/ 下，已被 data/ 忽略）

# 临时验证脚本
tools/private/_verify_copy_temp.py

# wechat_data 临时文件和 DLL（不提交到 git）
engine/wechat_data/dll/
engine/wechat_data/_db_key.tmp
engine/wechat_data/_get_key_output.txt
engine/wechat_data/_get_key_error.txt
